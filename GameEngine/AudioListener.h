#pragma once

#include "Component.h"

namespace TLGameEngine
{
	/// <summary>
	/// Audio Listener
	/// </summary>
	class AudioListener : public Component
	{
	public:
		AudioListener();
		virtual ~AudioListener();
	};
}